//----------------------------------------------------------------------------------
// [미션] children과 단방향 데이터 흐름 - 2026.08.29(토)
// [학습목표] children props 활용법 및 단방향 데이터 흐름 이해
//----------------------------------------------------------------------------------

//----------------------------------------------------------------------------------
// [핵심 개념]
// 1. children: 태그 사이의 내용물(<p>...</p>)을 받아오는 특별한 Props
// 2. 단방향 흐름: 데이터는 무조건 상위 -> 하위(App -> Profile -> Card)로 이동
// 3. 래퍼 패턴: 공통 틀(Card) 안에 내용만 바꿔 재사용하는 구조
//----------------------------------------------------------------------------------

//----------------------------------------------------------------------------------
// [1] Card 컴포넌트 (래퍼/틀)
// - 역할: 내용물(children)을 감싸서 렌더링하는 박스 역할
// - 원리: ({ children })으로 태그 사이 요소들을 쏙 받아옴
//----------------------------------------------------------------------------------
function Card({ children }) {
  return (
    <div className="card bg-base-200 shadow-sm">
      <div className="card-body">
        {children}
      </div>
    </div>
  );
}
//----------------------------------------------------------------------------------

//----------------------------------------------------------------------------------
// [2] Profile 컴포넌트 (합성)
// - 역할: 부모의 name과 children을 받아 Card 틀 안에 배치
// - 원리: 상위 컴포넌트에서 넘어온 데이터를 자식(Card)에게 전달 (단방향 흐름)
//----------------------------------------------------------------------------------
function Profile({ name, children }) {
  return (
    <Card>
      <h3 className="card-title">Profile</h3>
      <p>Name: {name}</p>

      <div className="alert">
        <span>{children}</span>
      </div>
    </Card>
  );
}
//----------------------------------------------------------------------------------

//----------------------------------------------------------------------------------
// [3] ChildrenPractice 실행용 컴포넌트
// - 역할: Profile 컴포넌트에 실제 props(name, children)를 넘겨서 화면에 보여줌
// - 원리: 학습 사이트는 <LessonComponent />만 실행하므로, 파일 내부에서 예시 props를 직접 전달
//----------------------------------------------------------------------------------
export default function ChildrenPractice() {
  return (
    <div className="space-y-4">
      <Profile name="React Learner">
        <p>children으로 전달된 내용입니다.</p>
      </Profile>
    </div>
  );
}
//----------------------------------------------------------------------------------
