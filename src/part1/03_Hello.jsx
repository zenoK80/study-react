//----------------------------------------------------------------------------------
// [미션] 첫 함수형 컴포넌트 만들기 - 2026.08.29(토)
// [학습목표] 컴포넌트 명명 규칙(PascalCase) 및 Fragment 문법 이해
//----------------------------------------------------------------------------------

//----------------------------------------------------------------------------------
// [핵심 개념]
// 1. PascalCase: 컴포넌트 이름 첫 글자는 무조건 대문자 (소문자는 HTML 태그 취급)
// 2. Fragment (<>...</>): 불필요한 <div> 태그 없이 여러 요소를 하나로 묶어줌
//----------------------------------------------------------------------------------

//----------------------------------------------------------------------------------
// [1] Hello 함수형 컴포넌트 선언 및 내보내기
// - 역할: 가장 기본이 되는 React 함수형 컴포넌트 생성 및 모듈 내보내기
// - 원리: 대문자로 시작하는 PascalCase(Hello)를 사용해야 React가 컴포넌트로 인식함
//----------------------------------------------------------------------------------
export default function Hello() {

  //--------------------------------------------------------------------------------
  // [2] React Fragment (<>...</>) 부모 감싸기
  // - 역할: 불필요한 DOM 요소(div 등)를 추가하지 않고 여러 자식 노드를 그룹화
  // - 원리: JSX는 단일 루트 노드만 반환해야 하므로 빈 태그 문법인 Fragment 사용
  //--------------------------------------------------------------------------------
  return (
    <>
      {/* -------------------------------------------------------------------------- */}
      {/* [3] UI 요소 반환 및 렌더링                                                 */}
      {/* - 역할: 화면에 표시할 제목(h1)과 설명 문구(p) 배치                         */}
      {/* - 원리: Fragment 내부 요소들이 실제 브라우저 DOM에 직접 렌더링됨          */}
      {/* -------------------------------------------------------------------------- */}
      <h1>Hello, React!</h1>
      <p>나의 첫 번째 React 컴포넌트입니다.</p>
    </>
  );
}
//----------------------------------------------------------------------------------
